# STChat Server Environment Variables

# Port to run the server on (required for cloud platforms)
PORT=3000

# Host to bind to (use 0.0.0.0 for containers/cloud)
HOST=0.0.0.0

# Allowed origins for CORS (comma-separated)
# Example: https://stchat.netlify.app,https://your-frontend.com
ALLOWED_ORIGINS=*
